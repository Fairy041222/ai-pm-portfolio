# AIPM Bench · AI产品经理作品集

> 一个帮助AI产品经理快速完成模型选型与Prompt验证的轻量级工具

**我的角色**：AI产品经理（需求定义、原型设计、模型评估、技术验证）  
**项目状态**：MVP开发中  
**PRD版本**：v1.0 | 更新日期：2026年5月

---

## 📌 项目概览

### 一句话介绍
AIPM Bench是一个轻量级Web工具，让AI PM在10分钟内完成模型选型与Prompt验证，替代原来2-4小时的手动对比。

### 核心解决痛点
- ❌ 无法快速验证哪个模型更好
- ❌ Prompt效果难以量化评估
- ❌ 手动对比耗时耗力

### 核心功能（MVP）
| 场景 | 功能 | 状态 |
|------|------|------|
| 模型选型 | 勾选多模型，自动生成5条测试用例，批量评测 | ✅ 本期 |
| Prompt优化 | 生成/优化Prompt，多版本对比 | ⏳ v1.1 |
| 评测报告 | 自动生成推荐、对比表、优缺点、成本预估 | ✅ 本期 |

---

## 🔍 需求发现

作为AI产品经理，能够在需求确定后、开发启动前，通过一个轻量级Web工具快速完成模型选型与Prompt验证——只需输入场景描述，系统自动生成测试用例并批量调用多个模型进行评测，在10分钟内输出可直接复制到PRD的标准化对比报告，从而解决手动对比效率低、结论难复用、跨部门对齐困难等痛点，同时要求单次评测成本控制在0.6元以内、操作步骤不超过5步、评测成功率达95%以上。

### 竞品分析

| 工具 | 优势 | 劣势 | 我们的差异化 |
|------|------|------|--------------|
| OpenAI Playground | 官方、免费 | 只能测OpenAI模型，无批量对比 | 多模型+自动化 |
| LangSmith | 功能强大 | 太重，需要部署，有学习成本 | 轻量、零配置 |
| PromptPerfect | Prompt优化强 | 不支持模型对比 | 两个场景都覆盖 |

## 📄 PRD（产品需求文档）

### 核心指标定义

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 评测耗时 | ≤60秒 | 3模型×3用例 |
| 评测成功率 | ≥95% | 成功/总次数 |
| 单次成本 | ≤¥0.6 | API调用总费用 |
| 操作步骤 | ≤5步 | 打开→开始评测 |

### MVP范围

**✅ 必须包含**
- 模型选型（多模型对比）
- AI自动生成3条测试用例
- 分析报告（推荐+对比表+成本）

**❌ 本期不做**
- 用户登录/账号
- Prompt优化场景（v1.1）
- 数据看板

### 完整BRD链接
📎 [AIPM Bench BRD.pdf](https://www.notion.so/AIPM-Bench-BRD-c31b1317da604ca79deda424be0fa667?source=copy_link)

### 完整PRD链接
📎 [AIPM Bench PRD.pdf](https://www.notion.so/AIPM-Bench-PRD-48e3f7328ef14f32b241414da7d6b806?source=copy_link)

---

## 🎨 原型设计

### 核心页面

**1. 场景输入与模型选择页**
> 输入任务描述，AI自动生成3条测试用例
> 勾选大模型选项

**2. 评测进度页**
> 实时显示：生成用例→评测中→生成报告

**3. 报告页**
> 推荐模型 + 对比表 + 成本分析

### Figma可点击原型
🔗 [AIPM Bench Figma](https://www.figma.com/design/NEyiquVyNR26AkhF6tbGcM/AIPM-Bench?node-id=0-1&t=SN8DHvEHVAHehX0w-1)

### 关键交互说明

| 交互点 | 规则 |
|--------|------|
| 输入框 | 占位符示例：“描述你的业务场景或粘贴需求文档，AI 将据此生成测试用例并评测所选模型...” |
| 进度条 | 每完成一个模型更新25%，显示“预计剩余X秒” |
| 失败提示 | “GPT-4o调用超时，已跳过，继续评测其他模型” |

---

## 🤖 模型评估与测试

### 测试集
> 问题：我现在需要做一个ai代码生成工具，用户可以提出需求，然后ai工具依据用户需求自动生成前后端代码。

| 类型 | 示例用例 | 实际输出 |
|------|----------|----------|
| 正常 | “我现在需要做一个ai代码生成工具，用户可以提出需求，然后ai工具依据用户需求自动生成前后端代码。” | 生成对应的模型测试报告 |
| 正常 | 用户勾选4个模型，全部Key有效 | 4个模型均完成评测，对比表含3行数据 |
| 正常 | “评测完成后点击“复制报告” | 复制成功，粘贴板内容为完整Markdown报告 |
| 正常 | 评测完成后点击“下载JSON” | 下载包含场景、用例、原始结果、报告的JSON文件 |
| 边界 | 场景描述输入框为空，点击开始评测 | 无法发送 |
| 边界 | 用户只勾选1个模型 | 提示“普通对话模式”，无法开始评测 |
| 异常 | 用户勾选的某个模型API Key无效或过期 | 报告中显示API Key问题 |
| 异常 | 评测过程中，某个模型单次调用超时 | 报告中显示超时问题 |
| 异常 | 评测过程中，所有模型全部调用失败 | 报告中显示出现的问题 |
| 异常 | 评测过程中，用户连续快速点击“开始评测” | 10秒内防重复点击，仅第一次生效，后续点击无响应 |
| 降级 | 评测中部分模型成功、部分失败 | 报告中成功模型正常展示对比，失败模型标注问题，推荐从成功模型中产生 |
| 安全 | 用户配置API Key时输入明文 | 输入框为password掩码，提交后后端加密存储，前端永不明文显示 |

### 模型对比实测报告

| 模型 | 成功率 | 平均耗时 | 单次成本 | 优点 | 缺点 |
|------|--------|----------|----------|------|------|
| DeepSeek | 100% | 8.8秒 | ¥0.05 | 通用业务场景准确率高，回答结构清晰、建议可执行 | 响应耗时偏长 |
| Hy3 preview | 100% | 14.0秒 | ¥0.07 | 通用业务场景准确率高，回答结构清晰、建议可执行 | 响应耗时偏长 |
| Qwen | 100% | 18.1秒 | ¥0.07 | 通用业务场景准确率高，回答结构清晰、建议可执行 | 响应耗时偏长 |
| 讯飞 Spark Lite | 100% | 4.9秒 | ¥0.03 | 通用业务场景准确率高，回答结构清晰、建议可执行 | 无 |

### Bad Cases

**📌 Case 1：API Key 未持久化，每次重启需重新输入**

❓ 问题描述
当重新运行整个项目时，用户为不同大模型配置的 API Key 没有被保存（例如仅存储在前端内存或未持久化到后端数据库），导致每次重启都需要重新输入，严重影响用户体验。

🔧 解决方案
将问题描述 + 期望行为整理为高精度 Prompt，提交给 Cursor 进行代码修复。核心要求：
- API Key 经加密后存储在后端数据库（使用强加密算法）
- 用户再次访问时，已配置的模型可直接使用，无需重新输入
- 前端仅存储“是否已配置”的布尔值，永不存储明文 Key

✅ 修复结果
用户配置的 Key 实现跨会话持久化，重启项目后无需重复输入。


**📌 Case 2：API Key 配置错误导致整个评测失败**

❓ 问题描述
初版 AIPM Bench 仅让用户输入大模型名称、URL 和 API Key，未对 Key 有效性进行前置校验。当某个模型的 Key 配置错误时，该模型的调用会失败，但错误处理不完善，有时会阻塞其他模型的评测，甚至导致整个流程中断。

🔧 解决方案
- 增加“验证 Key 有效性”按钮，允许用户在评测前主动校验
- 点击「开始评测」时，前端先校验所有勾选模型的 Key 是否有效（后端轻量调用）。若某个 Key 无效，弹窗明确列出失败模型，并阻止评测，引导用户重新配置
- 评测过程中，单个模型的调用失败（超时/Key 无效）自动重试 1 次，若仍失败则跳过该模型，继续执行其他模型的评测，最终报告中标注失败模型的状态

✅ 修复结果
Key 配置错误不再阻塞全流程，用户可清晰定位问题模型，其他模型的评测和报告生成不受影响。


**📌 Case 3：Markdown 报告内容过于简单，缺少详细分析**

❓ 问题描述
初版生成的 Markdown 报告仅简单显示“最优项”和“总结”，没有提供各模型的详细对比数据（如耗时、成本、成功率、每条用例的输出摘要等），导致用户无法判断推荐的依据，报告可用性低。

🔧 解决方案
扩展报告内容，在保留推荐结论和总结的基础上，增加：
- 各模型对比表：展示成功率、平均耗时、单次成本、优点/缺点
- 详细评分维度：基于成本、耗时、稳定性等客观指标计算得分
- 原始评测数据附录：可选展示每条用例的模型输出（或失败原因）
- 报告格式为 Markdown，支持一键复制，可直接粘贴到 PRD

✅ 修复结果
报告内容完整、可解释性强，用户不仅知道“推荐哪个模型”，还能看到“为什么推荐”，显著提升了信任度和复用率。

---

### 用户旅程
```mermaid
graph TD
    A[用户输入场景] --> B[AI自动生成5条用例]
    B --> C{用户编辑用例?}
    C -->|是| D[修改用例（1-10条）]
    D --> E{用例非空?}
    C -->|否| F[勾选≥2个模型]
    E -->|是| F
    E -->|否| D
    F --> G{所选模型Key有效?}
    G -->|否| H[提示配置Key]
    H --> F
    G -->|是| I[点击开始评测]
    I --> J[批量调用模型API]
    J --> K{是否有模型成功?}
    K -->|全部失败| L[提示评测失败，展示原始数据]
    K -->|部分成功| M[标记失败模型，继续]
    M --> N[基于规则生成报告]
    K -->|全部成功| N
    N --> O[展示报告+推荐+对比表]
    O --> P[用户复制/下载/保存历史]
    L --> Q[结束]
    P --> Q
